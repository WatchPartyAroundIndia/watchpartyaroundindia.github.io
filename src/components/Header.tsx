const Header = () => {
  return (
    <nav className="p-4 flex justify-between items-center backdrop-filter backdrop-blur-lg bg-opacity-30 fixed w-full top-0">
      <span className="text-lg font-medium text-white">
        Watch Party Around India
      </span>
    </nav>
  );
};

export default Header;
